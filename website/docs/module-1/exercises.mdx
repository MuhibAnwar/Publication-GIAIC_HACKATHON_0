---
title: Module 1 Exercises
description: Exercises for Module 1 on The Robotic Nervous System (ROS 2)
---

import LearningObjectives from '@site/src/components/LearningObjectives';
import Exercise from '@site/src/components/Exercise';

<LearningObjectives objectives={[
  "Implement ROS2 nodes with rclpy",
  "Design topics and services for humanoid control",
  "Configure launch files for multi-node systems",
  "Analyze ROS2 security implications"
]} />

# Module 1 Exercises

## Exercise 1: Node Communication

Create a simple ROS2 publisher and subscriber in Python. The publisher should send joint position commands at 50Hz, and the subscriber should log received commands with timestamp.

<Exercise 
  id="ex1" 
  type="coding" 
  question="Implement a ROS2 publisher that sends JointState messages containing 6 joint positions, and a subscriber that logs the received values with timestamps." 
  initialCode="# Publisher code
import rclpy
from rclpy.node import Node
from sensor_msgs.msg import JointState
import random

class JointCommandPublisher(Node):
    def __init__(self):
        super().__init__('joint_command_publisher')
        self.publisher = self.create_publisher(JointState, 'joint_commands', 10)
        self.timer = self.create_timer(0.02, self.publish_joint_commands) # 50Hz
        
    def publish_joint_commands(self):
        # TODO: Implement this method to publish random joint positions
        pass

def main(args=None):
    rclpy.init(args=args)
    node = JointCommandPublisher()
    rclpy.spin(node)
    node.destroy_node()
    rclpy.shutdown()" 
/>

---

## Exercise 2: Launch File Configuration

Configure a launch file that starts three nodes: a joint state publisher, a robot state publisher, and a custom controller node. Configure the robot description parameter.

<Exercise 
  id="ex2" 
  type="coding" 
  question="Create a launch file that starts the robot_state_publisher, joint_state_publisher, and a custom controller node. Include the robot_description parameter loaded from a URDF file." 
  initialCode="<?xml version='1.0'?>
<launch>
  <!-- Add your nodes here -->
</launch>" 
/>

---

## Exercise 3: Service Implementation

Implement a ROS2 service that calculates the Jacobian matrix for a simple 2-DOF planar manipulator given joint angles.

<Exercise 
  id="ex3" 
  type="coding" 
  question="Create a ROS2 service that accepts joint angles (theta1, theta2) and returns the 2x2 Jacobian matrix for a planar manipulator with link lengths l1=0.5 and l2=0.4." 
/>

---

## Exercise 4: System Architecture

**Question:** Explain how you would architect a ROS2 system for a humanoid robot with 28 degrees of freedom. What nodes would you create? What topics and services would you need? How would you handle coordination between perception, planning, and control?

<Exercise 
  id="ex4" 
  type="essay" 
  question="Describe your architecture for a 28-DOF humanoid robot using ROS2, including nodes, topics, services, and coordination mechanisms." 
/>

---

## Exercise 5: ROS1 vs ROS2 Comparison

**Question:** Compare and contrast ROS1 and ROS2 in the context of humanoid robotics. What advantages does ROS2 offer for controlling humanoid robots in human environments? What challenges might you face when migrating from ROS1 to ROS2?

<Exercise 
  id="ex5" 
  type="essay" 
  question="Compare ROS1 and ROS2 for humanoid robotics applications, focusing on advantages and migration challenges." 
/>